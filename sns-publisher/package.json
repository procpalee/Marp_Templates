{
  "name": "sns-publisher",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable CLI to publish card-news PNG carousels to Threads and PDF document shares to LinkedIn. Self-contained: depends only on @marp-team/marp-cli (for PDF rebuild) and dotenv.",
  "scripts": {
    "publish:cards": "cmd /c publish.cmd",
    "dry-run": "node scripts/index.js"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.2",
    "dotenv": "^16.6.1"
  },
  "engines": {
    "node": ">=18"
  }
}
