# 직접 편집·변환 가이드 (Obsidian_Vault 작업자용)

내가(사용자) 직접 마크다운을 고치고 슬라이드·카드뉴스·썸네일을 뽑는 방법. AI(`/marp`) 없이 터미널로 하는 법과, AI에게 시키는 법 둘 다.

---

## 0. 큰 그림 — 폴더 2개의 역할

| 폴더 | 역할 |
|---|---|
| `Obsidian_Vault/` | **내가 글·원고를 쓰는 곳** (옵시디언 노트, 이미지, 썸네일 소스) |
| `Marp_Templates/` | **변환 도구 + 테마**가 사는 곳. 실제 빌드 명령은 여기 `build/` 폴더에서 실행 |

터미널 명령은 **항상 `Marp_Templates/build` 폴더로 이동한 뒤** 실행한다:

```bash
cd C:/Users/PC/Documents/Github/Marp_Templates/build
```

준비물: Node.js만 있으면 됨 (marp-cli는 `npx --yes`가 자동 설치).

---

## 시나리오 A. 썸네일 만들기·수정 (제일 쉬움, AI 불필요)

### A-1. 이미 있는 썸네일 문구만 바꾸기

1. 옵시디언에서 소스 파일을 연다:
   `Obsidian_Vault/Attachments/thumbnails/<이름>.thumb.md`
2. 아래 3가지를 자유롭게 고친다:
   - `<div class="eyebrow">…</div>` → 상단 카테고리 (예: 업무자동화)
   - `# …` → 제목 (줄바꿈은 `<br>`)
   - `<p class="sub">…</p>` → 부제
3. 저장 후, `build/`에서 재렌더 (파일마다 소스 맨 위 주석에 명령이 적혀 있음):

```bash
npx --yes @marp-team/marp-cli "../../Obsidian_Vault/Attachments/thumbnails/<이름>.thumb.md" --image png --allow-local-files --theme-set ../themes/thumbnail/procpa-vivid -o "../../Obsidian_Vault/Attachments/thumbnails/<이름>.png"
```

> `--image`(단수)라서 `<이름>.png`가 바로 나온다. `--images`(복수)를 쓰면 `.001.png`가 붙으니 주의.

### A-2. 디자인 변형·배경·비율 바꾸기

소스 맨 위 주석(`<!-- _class: … -->`)에서:

- **변형 5종** — `_class`를 아래 중 하나로:
  | 변형 | 느낌 |
  |---|---|
  | `thumb-photo` | ★기본. 배경사진(꽉 채움) + 어두운 오버레이 + 흰 글씨 |
  | `thumb-typo` | 흰 배경 + 검정 타이포 + 상하 선 |
  | `thumb-ink` | 검정 배경 + 파랑 강조 바 |
  | `thumb-band` | 흰 배경 + 상단 파랑 띠 |
  | `thumb-badge` | 연재물용 회차 배지 (`<div class="badge">EP 01</div>` 추가) |
- **배경사진** — `_backgroundImage: url('../thumb-bg/<파일>.jpg')` 의 url만 바꾼다. (어두운 스크림·PROCPA 로고는 테마가 자동으로 넣으니 url만 넣으면 됨)
- **비율** — 기본 16:9. 인스타 정사각이 필요하면 `size: 16:9`를 `size: sq`로 + `_class`에 `sq` 추가. 카드 표지 4:5는 `size: sns` + `tall`.

### A-3. 새 썸네일을 처음부터

`Marp_Templates/themes/thumbnail/procpa-vivid/sample-169.md`를 복사해 원하는 위치(`Attachments/thumbnails/`)에 `<새이름>.thumb.md`로 저장 → 문구·배경 고치고 A-1 명령으로 렌더.

---

## 시나리오 B. 이미 만든 슬라이드 덱을 고쳐서 다시 빌드 (AI 불필요)

이미 `.marp.md`(또는 `.md`)로 변환된 덱의 **문구·순서만 손보고** HTML/PDF를 다시 뽑을 때.

1. 옵시디언에서 변환된 슬라이드 md를 연다 (예: `.../Slides/…/kicpa-claude-excel-dcf.md`).
2. 텍스트를 고친다. 레이아웃을 바꾸려면 슬라이드 맨 위 `<!-- _class: … -->`를 CORE 목록(아래) 안에서 교체.
3. `build/`에서:

```bash
# HTML (검수용)
npx --yes @marp-team/marp-cli "<슬라이드.md 경로>" --html --allow-local-files --theme-set ../themes/slide -o "<출력.html 경로>"

# PDF (배포용)
npx --yes @marp-team/marp-cli "<슬라이드.md 경로>" --pdf --allow-local-files --theme-set ../themes/slide -o "<출력.pdf 경로>"
```

**procpa-vivid CORE 레이아웃** (이 목록 안에서만 쓰기 — 나머지는 아카이브):
- 셸: `cover` · `cover-image` · `cover-split` · `section` · `toc` · `end` · `qa` · `thanks-contact` · `session-break`
- 본문: `split` · `feature-cards` · `content-sidebar` · `comparison-vs` · `comparison` · `steps` · `icon-list` · `checklist` · `faq` · `vertical-timeline`
- 임팩트(장당 1~3번만): `statement` · `takeaway` · `callout-hero`
- 컴포넌트로 조립하는 법은 [vivid-recipes.md](.claude/skills/md-to-marp-propca/references/vivid-recipes.md) R-01~R-12.

---

## 시나리오 C. 새 글을 슬라이드/카드뉴스로 "처음" 변환 (AI 사용)

옵시디언 원고를 레이아웃까지 알아서 짜서 슬라이드로 만드는 건 **AI 판단이 필요**하므로 이건 Claude에게 시킨다. Claude Code 채팅창에:

```
/marp "<원고 md 경로>" 강의용
```

- 용도 키워드로 자동 분기:
  - `강의/교육/세미나/임원` → 발표 덱 (톤 자동 선택)
  - `인스타/카드뉴스/쓰레드/링크드인` → 4:5 카드뉴스 (PNG, 링크드인이면 PDF도)
  - `썸네일` → 썸네일 PNG
  - `풀세트` → **덱 + 카드뉴스 + 썸네일 한 번에** (원소스 멀티산출)
- procpa-vivid 테마로 만들려면 "procpa-vivid로"라고 덧붙이면 됨.
- 결과는 `Marp_Templates/output/<이름>/`에 저장되고, 자동 QA까지 돈다.

> 원고를 변환이 잘 되게 쓰는 법(제목 구조·불릿·이미지 배치)은 [authoring-guide.md](.claude/skills/md-to-marp-propca/references/authoring-guide.md) 참고.

---

## 자주 쓰는 명령 요약

```bash
cd C:/Users/PC/Documents/Github/Marp_Templates/build

# 썸네일 재렌더 (단수 --image = 파일명 그대로)
npx --yes @marp-team/marp-cli "<썸네일.thumb.md>" --image png --allow-local-files --theme-set ../themes/thumbnail/procpa-vivid -o "<출력.png>"

# 슬라이드 덱 → HTML
npx --yes @marp-team/marp-cli "<덱.md>" --html --allow-local-files --theme-set ../themes/slide -o "<출력.html>"

# 슬라이드 덱 → PDF
npx --yes @marp-team/marp-cli "<덱.md>" --pdf --allow-local-files --theme-set ../themes/slide -o "<출력.pdf>"

# 테마 자체 샘플 미리보기 빌드 (npm 스크립트)
npm run build:procpa-vivid      # 슬라이드 테마 데모
npm run thumb:all               # 썸네일 3비율 데모
```

## 막힐 때

- **이미지가 안 나옴** → `--allow-local-files` 빠졌는지 확인.
- **테마가 적용 안 됨(밋밋함)** → `--theme-set` 경로 확인. 썸네일은 `../themes/thumbnail/procpa-vivid`, 슬라이드는 `../themes/slide`.
- **`.001.png`가 붙음** → `--images`(복수)를 `--image`(단수)로.
- **한글이 깨짐/폰트 이상** → 인터넷 연결 확인(Pretendard 폰트를 CDN에서 불러옴).
- **레이아웃 이름을 모르겠음** → [layouts-by-purpose.md](themes/slide/procpa-vivid/layouts-by-purpose.md)에서 용도별로 찾기.
